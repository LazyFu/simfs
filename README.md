# Simfs

Simfs — a simple user-space file system simulator.

A minimal file system implementation that runs in user space.

## Usage

Run `make` to build the `mkfs` utility. Then run:

- `./mkfs` — create the default file system image `simfs.img`
- `./mkfs image.img` — create a file system image with the specified filename.
